name = "nexcraft-chat-proxy"
main = "worker.js"
compatibility_date = "2025-01-01"

# Run `wrangler kv:namespace create BOOKINGS` once, then paste the returned id below.
kv_namespaces = [
  { binding = "BOOKINGS", id = "REPLACE_WITH_KV_NAMESPACE_ID" },
]
